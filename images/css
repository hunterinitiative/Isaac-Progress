/* styles.css */
.container {
    max-width: 1200px; /* Set a maximum width for the container (adjust as needed) */
    margin: 0 auto; /* Center the container horizontally */
    padding: 20px; /* Add some padding to create space around the content */
}

.content {
    
}

.image-container {
    float: right; /* Float the image to the right side */
    width: 500px; /* Set the width of the image container (adjust as needed) */
    /* You can also add some margin or padding if you want space around the image */
}
